services:
  - type: web
    name: trading-econ-bot
    env: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y chromium chromium-driver
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python main.py
