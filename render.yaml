services:
  - type: web
    name: trading-econ-bot
    env: python
    plan: free
    runtime: python3.14   # Python 3.14 compatibile con v20.3+
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python main.py
